    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        #state going up
        if not head:
            return None
        
        curr = head##state going down
        if head.next:
            curr = self.reverseList(head.next)
            head.next.next = head
        head.next = None

        return curr
        
